.pio

# Контейнер OTA собирается автоматически (tools/build_container.py)
# при каждой сборке — это артефакт, а не исходник.
greenhouse_v*.bin

.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Локальные настройки инструментов — содержат абсолютные пути этой машины
.claude/
.vscode/settings.json
