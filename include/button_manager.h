#pragma once
// ╔══════════════════════════════════════════════════════════════════╗
// ║  button_manager.h — Обработка физических кнопок v5.0            ║
// ║                                                                  ║
// ║  5 кнопок (было 7 в v4):                                         ║
// ║   • 3 кнопки полива (индексы 0..2)                              ║
// ║   • 2 кнопки форточек (индексы 3..4)                            ║
// ║                                                                  ║
// ║  Убраны в v5: кнопки увлажнителя и насоса — их пины (4, 15)     ║
// ║  теперь заняты реле H-bridge форточек.                           ║
// ║                                                                  ║
// ║  Принцип: ISR с аппаратным антидребезгом (BTN_DEBOUNCE_US),     ║
// ║  события через std::atomic bitmask, обработка в задаче Core1.   ║
// ╚══════════════════════════════════════════════════════════════════╝

#include "config.h"
#include <stdint.h>
#include <stdbool.h>

namespace BtnMgr {

    // Настройка GPIO как INPUT_PULLUP, регистрация ISR на FALLING.
    // Вызвать один раз в setup().
    void init();

    // Вызывается в цикле taskCore1 (~50 мс).
    // Атомарно читает и сбрасывает bitmask событий от ISR,
    // обрабатывает каждое нажатие.
    void processEvents();

}  // namespace BtnMgr
