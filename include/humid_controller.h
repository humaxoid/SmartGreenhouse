#pragma once
// ╔══════════════════════════════════════════════════════════════════╗
// ║  humid_controller.h — Управление увлажнителем v5.0              ║
// ║                                                                  ║
// ║  Гистерезис по влажности:                                        ║
// ║   • H <= humidLow   → реле ВКЛ  (начать увлажнение)             ║
// ║   • H >= humidHigh  → реле ВЫКЛ (целевая достигнута)            ║
// ║   • low < H < high  → сохранять текущее состояние               ║
// ║                                                                  ║
// ║  При NaN датчика реле выключается (безопасное поведение).       ║
// ╚══════════════════════════════════════════════════════════════════╝

#include <stdint.h>
#include <stdbool.h>

namespace HumidCtrl {

    // Вызывать каждый цикл taskCore1 (~1 сек достаточно).
    //
    // Параметры:
    //   humidity — текущая влажность, % (NaN = датчик недоступен)
    //
    // В auto-режиме управляет реле RELAY_IDX_HUMID по гистерезису.
    // В ручном режиме — ничего не делает (пользователь сам управляет).
    void update(float humidity);

}  // namespace HumidCtrl
