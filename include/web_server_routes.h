#pragma once
// ╔══════════════════════════════════════════════════════════════════╗
// ║  web_server_routes.h — HTTP эндпоинты веб-панели v5.0           ║
// ║                                                                  ║
// ║  Регистрирует:                                                   ║
// ║   • Статику из LittleFS (/, /app.js, /style.css, /chart.js)     ║
// ║   • REST API для настроек сети, MQTT, учётных данных            ║
// ║   • WebSocket (через WsHandler::attachTo)                        ║
// ║   • OTA эндпоинт (через OtaMgr::attachTo)                        ║
// ║                                                                  ║
// ║  Все эндпоинты (кроме OTA и статики с заголовком Cache-Control)  ║
// ║  требуют HTTP Basic Auth.                                        ║
// ╚══════════════════════════════════════════════════════════════════╝

namespace WebRoutes {

    // Инициализация AsyncWebServer на порту 80.
    // Регистрирует все маршруты и запускает server.begin().
    // Вызывать ПОСЛЕ WsHandler::init() и OtaMgr::init().
    void init();

    // Пора ли выполнить отложенную перезагрузку, запрошенную из веб-панели
    // (смена сетевых настроек, сброс к заводским, ручной перезапуск).
    // Опрашивается из loop(); отдельной задачи под это больше нет.
    bool rebootDue();

}  // namespace WebRoutes
